name: ci

on:
  push:
    branches: [master]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: lazytest
        ports:
          - 3306:3306
        options: >-
          --health-cmd "mysqladmin ping -uroot -proot"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    env:
      LAZYDB_MYSQL_DSN: root:root@tcp(127.0.0.1:3306)/lazytest?timeout=5s
      LAZYDB_SSH_HOST: 127.0.0.1
      LAZYDB_SSH_PORT: "2222"
      LAZYDB_SSH_USER: runner
      LAZYDB_SSH_KEY: /home/runner/.ssh/id_ed25519
      LAZYDB_SSH_TARGET_HOST: 127.0.0.1
      LAZYDB_SSH_TARGET_PORT: "3306"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.27"
      - name: Install and start sshd (tunnel test target)
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq openssh-server
          ssh-keygen -t ed25519 -N '' -f ~/.ssh/id_ed25519
          cat ~/.ssh/id_ed25519.pub >> ~/.ssh/authorized_keys
          chmod 600 ~/.ssh/authorized_keys
          sudo mkdir -p /run/sshd
          sudo /usr/sbin/sshd -p 2222
      - name: Vet
        run: go vet ./...
      - name: Test (unit + MySQL/SSH integration, SQLite test source)
        run: go test -count=1 -timeout 300s ./...
      - name: Build CGO_ENABLED=0
        run: CGO_ENABLED=0 go build ./...
  matrix:
    # 驱动代理与主程序的三平台产物（#22 ADR-0002）
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: macos-latest, goos: darwin, goarch: amd64 }
          - { os: macos-latest, goos: darwin, goarch: arm64 }
          - { os: ubuntu-latest, goos: linux, goarch: amd64 }
          - { os: windows-latest, goos: windows, goarch: amd64 }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.27"
      - name: Build driver agent + main
        run: |
          $env:CGO_ENABLED = "0" ; go build -o lazydb-driver-${{ matrix.goos }}-${{ matrix.goarch }} ./cmd/lazydb-driver ; go build -o lazydb-${{ matrix.goos }}-${{ matrix.goarch }} ./cmd/lazydb
        if: matrix.goos == 'windows'
        shell: pwsh
      - name: Build driver agent + main
        run: |
          CGO_ENABLED=0 go build -o lazydb-driver-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.goos == 'windows' && '.exe' || '' }} ./cmd/lazydb-driver
          CGO_ENABLED=0 go build -o lazydb-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.goos == 'windows' && '.exe' || '' }} ./cmd/lazydb
        if: matrix.goos != 'windows'
        shell: bash
      # 三平台各跑通 MySQL 直连单链路（#25）：本机 arch 才跑，交叉项只验编译
      - name: MySQL single-link test (native arch)
        if: matrix.goos == 'linux' || (matrix.goos == 'darwin' && matrix.goarch == 'arm64') || matrix.goos == 'windows'
        shell: bash
        run: |
          set -e
          if [[ "$RUNNER_OS" == "macOS" ]]; then
            brew install mysql >/dev/null
            brew services start mysql
            sleep 5
            mysql -uroot -e "CREATE DATABASE IF NOT EXISTS lazytest;"
            DSN="root@tcp(127.0.0.1:3306)/lazytest?timeout=5s"
          elif [[ "$RUNNER_OS" == "Linux" ]]; then
            sudo apt-get update -qq && sudo apt-get install -y -qq mysql-server
            sudo service mysql start
            sleep 2
            sudo mysql -e "CREATE DATABASE IF NOT EXISTS lazytest;"
            sudo mysql -e "ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'root'; FLUSH PRIVILEGES;"
            DSN="root:root@tcp(127.0.0.1:3306)/lazytest?timeout=5s"
          else
            docker run -d --name mysql -p 3306:3306 -e MYSQL_ROOT_PASSWORD=root -e MYSQL_DATABASE=lazytest mysql:8
            for i in $(seq 1 60); do docker exec mysql mysqladmin ping -uroot -proot --silent && break; sleep 2; done
            DSN="root:root@tcp(127.0.0.1:3306)/lazytest?timeout=5s"
          fi
          LAZYDB_MYSQL_DSN="$DSN" go test -count=1 -timeout 120s -run 'TestMySQLFullChainDirect' ./internal/api/
      - uses: actions/upload-artifact@v4
        with:
          name: lazydb-${{ matrix.goos }}-${{ matrix.goarch }}
          path: |
            lazydb-driver-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.goos == 'windows' && '.exe' || '' }}
            lazydb-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.goos == 'windows' && '.exe' || '' }}
